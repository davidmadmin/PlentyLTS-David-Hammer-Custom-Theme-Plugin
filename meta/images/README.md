# Bilder für Plugin-Metadaten

In diesem Ordner liegen die aktuell verwendeten Bilddateien für die Plugin-Metadaten:

- `David_Abdin_author_icon.jpg` (für `authorIcon`)
- `davids_custom_plentylts_theme_logo.png` (für `pluginIcon`)

## Zuordnung in `plugin.json`
Die beiden Bilder sind in `plugin.json` direkt verdrahtet:

- `authorIcon`: `David_Abdin_author_icon.jpg`
- `pluginIcon`: `davids_custom_plentylts_theme_logo.png`

Wenn Du die Dateien austauschst, passe bitte immer die Dateinamen in `plugin.json` synchron an.
